// 선택된 상품 여러개 삭제
$('.deleteSeveralBtn').on('click', function () {
  // 동적으로 상품 번호 여러개 넣어주는 태그 생성
  // 인덱스 정의
  let formContents = '';
  let deleteNumber = 0;

  // 체크 박스 순회
    // 체크된 요소 찾기
      // 상품번호 조회
      // 인풋태그 생성
      // form에 추가
      // 인덱스 + 1
  $('.item-checkbox').each(function (i, e) {
    alert('hi');
  })

  // form 전송
})